# Yoga-Pose-Detection
Yoga Pose Detection project that uses Convolutional Neural Networks (CNNs) to classify yoga poses . The repository includes data preprocessing scripts, model training notebook, saved model, and a Flask web app for local deployment. The goal is to assist users in correctly identifying poses and enhancing posture learning.
